# Le moteur d'options est en Rust : voir options-rs/ et son README.
# Ce qui reste en Python est sec_data.py, les declarations d'inities SEC.
#   pip install -e ".[dev]"         tests
pandas==3.0.5
requests==2.34.2
