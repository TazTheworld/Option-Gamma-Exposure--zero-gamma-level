# Le necessaire pour les actions et indices US (source CBOE).
# Les autres sources sont des extras : voir pyproject.toml.
#   pip install -e ".[databento]"   chaines CME par API
#   pip install -e ".[cme]"         exports CME au format Excel
#   pip install -e ".[barchart]"    scraping Barchart (selenium)
#   pip install -e ".[snapshots]"   archivage en parquet plutot qu'en csv.gz
#   pip install -e ".[dev]"         tests
matplotlib==3.11.1
numpy==2.5.1
pandas==3.0.5
requests==2.34.2
scipy==1.18.0
